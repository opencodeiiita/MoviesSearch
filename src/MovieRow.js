import React from 'react';
import ReactDOM from 'react-dom';

class MovieRow extends React.Component {
  render() {
    return <h2>MovieRow</h2>;
  }
}

ReactDOM.render(<MovieRow />, document.getElementById('MovieRow'));